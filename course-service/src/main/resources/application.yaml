spring:
  application:
    name: course-service
  datasource:
    url: jdbc:postgresql://localhost:5432/course_service_db
    username: postgres
    password: huylebron2828
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/